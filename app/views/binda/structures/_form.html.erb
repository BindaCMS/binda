<div class="row">
<div class="form-inputs col-sm-12">

<%= simple_form_for structure , defaults: { label: false } do |f| %>
  <% if structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize( structure.errors.count, "error") %> prohibited this structure from being saved:</h2>

      <ul>
      <% structure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-sm-8">

      <div class="form-inputs--row row">
        <div class="col-sm-12">

          <h4>Page Details</h4>
          
          <h5>Title</h5>
          <%= f.input :name %>

        </div>
      </div>

    </div>
    <div class="col-sm-4">
      <div class="form-inputs--row row">
        <div class="col-sm-12">
          <div class="form-actions">
            <h4>Setting</h4>
            <hr>
            <%= f.button :submit, 'Save changes', class: 'btn btn-primary' %>
          </div>
        </div>
      </div>
    </div>
<% end %>

</div>
</div>