<%# - - - - - - - - - - - - %>
<%# TEXT / STRING           %>
<%# - - - - - - - - - - - - %>

<% if repeater_setting_child.field_type == 'text' || repeater_setting_child.field_type == 'string' %>
  <%# current = repeater.texts.build( field_setting: repeater_setting_child ) %>
  <%=f.simple_fields_for "texts_attributes[]", repeater.find_or_create_a_field_by( repeater_setting_child.id, 'text' ) do |ff| %>
		<%# ff.input :repeater_id, as: :hidden, input_html: { value: repeater.id } %>
    <%= render 'form_item_text', ff: ff, field_setting: repeater_setting_child %>
  <% end %>
<% end %>