require 'rails_helper'

module Binda
  RSpec.describe Image, type: :model do

		# it "must be associated to a component or a board" do
		# 	image = Image.new
		# 	expect{image.save}.to raise_error ActiveModel::ValidationError
		# 	image_setting = build_stubbed(:image_setting)
		# 	image.field_setting_id = image_setting.id
		# 	expect{image.save}.to raise_error ActiveModel::ValidationError
		# 	image_component = build_stubbed(:component)
		# 	image.fieldable_id = image_component.id
		# 	expect{image.save}
		# end

  end
end
